# AMAZING_SUDOKU

Sudoku is solved by starting from the image and capturing all the values, eventually returning the answer as a finished image.

The libraries are used: 
from PIL import Image
pytesseract import *
cv2
numpy
matplotlib
